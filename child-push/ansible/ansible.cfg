[defaults]
# standard path
roles_path = roles
inventory = inventory/hosts.yml

# remove verbose checks
host_key_checking = True
deprecation_warnings = False

[ssh_connection]
# pipeline to speed up ssh tasks
pipelining = True
